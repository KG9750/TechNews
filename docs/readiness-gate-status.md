# Development Readiness Gate Status

Status: Passed
Last updated: 2026-06-08

This file is the current audit trail for the readiness gate in `docs/PRE-DEVELOPMENT-PLAN.md`. It records evidence that has been verified before formal MVP implementation issues move out of `needs-triage`.

## Gate Summary

The repository has completed the required live external validation. Delivery schedule, Feishu delivery, Model Provider, Archive/storage, and source-owner narrowing inputs are now resolved in the secure local runtime/evidence set. Ignored runtime files remain local only: do not commit `.env`, `evidence/`, or `archives/`.

Final manifest and gate execution passed on 2026-06-08:

- `evidence/readiness-manifest.json` was generated by `scripts/spikes/readiness_manifest.py --write-final-review-packet`.
- `python3 scripts/spikes/live_readiness_preflight.py --strict` passed with 0 missing environment variables, 0 invalid environment values, 0 missing evidence files, 0 partial final evidence groups, and 0 live evidence validation failures.
- `python3 scripts/check_readiness.py --require-live --require-evidence` passed.
- `python3 scripts/check_readiness.py --require-github` passed.

Additional review item:

- Source eligibility: 32 first-version seed sources are covered in `docs/source-eligibility-reviews.md`; all first-version sources now have source-specific evidence or per-URL review rules recorded, and `scripts/check_readiness.py` rejects generic terms-evidence placeholders. The MVP source set is narrowed to 7 eligible metadata-only production sources. The other 25 first-version sources are marked `deferred`, excluded from MVP production auto-ingestion, and require the owner review to be reopened before any later production use. `fixtures/source-ingestion/source-access-policy.json` mirrors that narrowed state, and `fixtures/source-ingestion/source-owner-review-queue.json` now has 0 open queue items.

Current source-owner decision status is resolved by MVP source-set narrowing: 7 eligible sources can be used by metadata-only production connectors, 25 sources are deferred, and the tracked owner review queue is empty. GitHub issue #21 tracks the historical approval queue and can be updated with a safe summary after the final manifest/gate passes.

The live spike evidence procedure is now documented in `docs/live-spike-evidence-runbook.md`. Template evidence files live under `fixtures/live-evidence-templates/`; they are examples only, and `scripts/check_readiness.py` rejects any live evidence that still contains `TEMPLATE_` placeholders or common sensitive leak patterns.

Live spike runners, the preflight packet, the action packet, and the final readiness gate all load root `.env` when present; process environment values take precedence, and `.env` remains ignored by git.

Final live evidence must also include `evidence/readiness-manifest.json`, which declares the evidence set and checks model/archive run metadata consistency; `scripts/spikes/readiness_manifest.py` generates that manifest from the redacted evidence files and can write `evidence/final-redaction-review.md` as an ignored operator checklist before sharing issue updates.

`scripts/spikes/live_readiness_preflight.py` provides a one-command preflight that runs local helper dry-runs, validates model-provider request envelopes as metadata-only, reports missing environment variable names, lists missing evidence files, groups final evidence by spike as `missing`, `partial`, or `complete`, inventories dry-run artifacts separately from final evidence, runs the same live evidence validator used by the final gate, and can write ignored Markdown execution packets without printing secret values, including per-spike packets for Feishu, model-provider, and archive-storage issue updates. A `partial` final evidence group means the operator must finish that spike before closing the linked issue or moving readiness forward.

Strict preflight runs the local helper dry-runs by default, so `--strict --write-packet --write-spike-packets` checks both existing final evidence and the local dry-run helper path before the final gate.

`scripts/readiness_action_packet.py --write-source-owner-packets --write-mvp-issue-packets` then generates or links the live packet, per-spike packets, readiness-manifest blocker, final redaction review packet, external input request checklist and ignored `external-input-request.md`, ignored `github-update-packet.md` with copy-safe issue comments, label guardrails, per-MVP issue follow-up comments, and per-spike evidence validator commands, ignored `delivery-schedule-decision.md`, GitHub issue links, source-owner review index, source-owner worksheet, source-owner batch plan, source-owner request packet, MVP source-set narrowing packet, an MVP issue unlock matrix, and ignored per-MVP-issue triage packets into one top-level execution path. Synthetic leaky fixtures under `fixtures/live-evidence-negative/` prove the redaction scanner fails unsafe Feishu, Model Provider, and Archive/storage evidence. The Feishu spike runner also redacts common sensitive response keys and values before writing evidence, documents a custom group bot fallback shape, and keeps fallback evidence separate from the required internal-app user and group gate.

Pre-development readiness is also protected in GitHub Actions by `.github/workflows/pre-development-readiness.yml`. The workflow uses Node 24-native GitHub actions, runs the local readiness checker, compiles the readiness/spike scripts, and proves the tracked evidence templates cannot pass as live evidence. It intentionally does not require live external credentials.

The GitHub issue tracker gate is checked manually because it requires `gh` access. Run it from this checkout with:

```bash
python3 scripts/check_readiness.py --require-github
```

This verifies GitHub auth, origin remote identity, default branch, push-capable permission, required triage labels, milestones, pre-development issues including source-owner approval issue #21, and MVP issue labels. The default CI workflow intentionally does not require this external access.

Local evidence can be checked with:

```bash
python3 scripts/check_readiness.py
```

To make missing external variables fail the check before a live spike, run:

```bash
python3 scripts/check_readiness.py --require-live
```

To make missing or invalid redacted live evidence fail the check after live spikes, run:

```bash
python3 scripts/check_readiness.py --require-evidence
```

For the final readiness gate, require both environment variables and redacted evidence:

```bash
python3 scripts/check_readiness.py --require-live --require-evidence
```

The checker may also print `REVIEW` notes. These are not local fixture failures, but they identify items that must not be treated as fully production-ready. For example, first-version seed sources still need source-by-source eligibility review before automated ingestion.

## Requirement Status

| Requirement | Status | Evidence | Remaining gap |
| --- | --- | --- | --- |
| Baseline docs are committed and pushed | Passed | Baseline commit `6fa77b1`; current `main` is verified by the latest Pre-development readiness CI and optional GitHub tracker gate | None |
| GitHub repo access, labels, milestones, and tracking issue exist | Passed | `--require-github` verifies `gh auth status`, origin remote identity, default branch `main`, push-capable repo permission, labels, milestones, tracking issue #1 pre-readiness and post-merge closure state, live-spike blockers #3/#5/#6, and source-owner approval blocker #21 | None |
| Pre-development readiness CI exists | Passed | `.github/workflows/pre-development-readiness.yml`; `scripts/check_readiness.py` verifies Node 24-native action versions, workflow coverage, secrets inventory alignment, live evidence helper coverage, and local fixtures | None |
| Minimum contracts exist | Passed | `docs/schemas/minimal-contracts.md`; readiness verifies required contract sections, core fields, guardrail rules, and spike references; issue #2 closed | None |
| Exact daily Delivery Deadline is configured | Passed | Secure local runtime is configured for `08:00` in `Asia/Beijing`; `docs/MVP-SCOPE.md`; `.env.example`; `docs/secrets.md`; live preflight and action packets list `DELIVERY_DEADLINE_LOCAL_TIME` and `DELIVERY_TIMEZONE` | None |
| Feishu delivery spike passes for one user and one group | Passed | Fixtures in `fixtures/feishu-delivery/`; live evidence template in `fixtures/live-evidence-templates/feishu-delivery/`; readiness validates source line, Confidence Notice, and Archive/Deep-Dive link in rendered evidence; live internal-app send evidence is present under ignored `evidence/feishu-delivery/`; canonical bot name is “新闻简报秘书”; the configured delivery chat is recorded only in the secure local runtime and redacted evidence; final manifest declares the redacted Feishu evidence files | None |
| Source ingestion spike normalizes every First-Version Source type | Passed | `docs/spikes/source-ingestion.md`; `fixtures/source-ingestion/candidate-items.json`; issue #4 closed | None |
| Archive/storage spike passes | Passed | Local archive fixture in `fixtures/archive-storage/`; live evidence template in `fixtures/live-evidence-templates/archive-storage/`; live sync success evidence is present under ignored `evidence/archive-storage/`; final manifest declares the redacted archive evidence files | None |
| Model Provider spike passes structured output and usage metadata checks | Passed | Prompt/output fixtures in `fixtures/model-provider/`; model request envelopes validate as metadata-only with `scripts/spikes/model_provider_spike.py --validate-requests`; live evidence template in `fixtures/live-evidence-templates/model-provider/`; live provider/model output and usage evidence is present under ignored `evidence/model-provider/`; final manifest declares the redacted model evidence files and matches usage metadata | None |
| Live evidence runbook and template-negative gate exist | Passed | `docs/live-spike-evidence-runbook.md`; `scripts/spikes/live_readiness_preflight.py`; `scripts/spikes/readiness_manifest.py`; `scripts/readiness_action_packet.py`; `scripts/test_live_evidence_helpers.py`; `fixtures/live-evidence-templates/`; `fixtures/live-evidence-templates/readiness-manifest.json`; `fixtures/live-evidence-negative/leaky-feishu/`; `fixtures/live-evidence-negative/leaky-model-provider/`; `fixtures/live-evidence-negative/leaky-archive-storage/`; `scripts/check_readiness.py --require-evidence --evidence-root fixtures/live-evidence-templates` fails on `TEMPLATE_` placeholders; live evidence scans for common token/id/path leaks and raw sensitive environment values; preflight validates model request envelopes, reports missing env/evidence, reports final evidence group status including partial groups, summarizes live evidence validation failures, inventories dry-run artifacts separately from final evidence, and writes a Markdown execution packet plus per-spike issue-facing packets without secret values; the top-level action packet can generate and link live evidence, per-spike packets, readiness manifest, final redaction review packet, external input request checklist and ignored `external-input-request.md`, ignored `github-update-packet.md` with copy-safe issue comments, label guardrails, per-MVP issue follow-up comments, and per-spike evidence validator commands, ignored `delivery-schedule-decision.md`, GitHub issue links, source owner review index, worksheet, batch plan, request packet, MVP source-set narrowing packet, source owner review state, an MVP issue unlock matrix, and per-MVP-issue triage packet directory; final evidence manifest checks declared files, model/archive run metadata, current git HEAD match, and a clean tracked worktree; final redaction review packet lists required evidence files, share guardrails, per-spike evidence validator commands, and fields that must remain visible; Feishu, Model Provider, and Archive/storage leaky evidence roots must fail the negative gate; Feishu runner redaction and explicit group fallback separation, archive failure reason redaction, helper regressions, model request guardrail tests, and a synthetic redacted positive evidence package are covered by readiness | None |
| Technology Domain Template and briefing style guide are drafted | Passed | `docs/taxonomy/technology-domain-template.md`; `docs/briefing-style-guide.md`; readiness verifies MVP sections, Embodied Intelligence subcategories, Push Briefing structure, confidence notices, source/media rules, and Deep-Dive Detail rules; issue #7 closed | None |
| Source registry contains at least 30 seed sources | Passed | `docs/source-registry.md`; 37 total seed sources, 32 first-version, 5 deferred | None |
| Source eligibility review covers first-version seed sources | Passed | `docs/source-eligibility-reviews.md`; `fixtures/source-ingestion/source-access-policy.json`; `fixtures/source-ingestion/source-owner-review-queue.json`; `docs/source-owner-review-runbook.md`; `scripts/source_owner_review_decision.py`; `scripts/test_source_owner_review_decision.py`; 32 first-version sources covered, 7 `eligible`, 25 `deferred`; tracked owner review queue has 0 open items; deferred sources have `production_auto_ingestion: false`; owner review tooling remains available for later source reopening | None for MVP source-set narrowing |
| Golden samples cover at least 20 real-world items | Passed | `fixtures/golden-samples/items.json`; 22 total fixtures, 21 real-world items; issue #8 closed | None |
| Required ADRs exist | Passed | 8 ADRs in `docs/adr/`; readiness verifies all 7 required architecture ADRs are `accepted`, with tradeoffs, consequences, decision keywords, and matching `docs/ARCHITECTURE-NOTES.md` defaults; issue #9 closed | None |
| MVP issue breakdown exists and is labeled | Passed | `docs/github-issue-breakdown.md`; `docs/issues/mvp/`; `scripts/readiness_action_packet.py --write-mvp-issue-packets`; readiness verifies 11 mapped drafts, required sections, relevant doc paths, dependency sections, post-readiness labels, acceptance coverage map, MVP issue unlock matrix, and ignored issue-specific triage packet generation; issues #10, #11, #12, #13, and #14 are complete; GitHub issues #15-#19 remain `ready-for-agent`; #20 remains `needs-triage` until core MVP implementation completes | None |

## External Input Evidence

Live spike values are now present in the secure local test environment, live evidence has been generated under ignored `evidence/`, and the source-owner approval queue has been resolved by MVP source-set narrowing. Do not commit credentials, recipient ids, local paths, provider responses, private review notes, `evidence/`, or `archives/`.

Use `docs/live-spike-evidence-runbook.md` for the exact commands, redaction rules, evidence layouts, and final gate command. Copy templates from `fixtures/live-evidence-templates/` only as a starting point; every `TEMPLATE_` placeholder must be replaced or the checker will fail.

For a fresh live execution, run:

```bash
python3 scripts/spikes/live_readiness_preflight.py --dry-run --write-packet --write-spike-packets
python3 scripts/readiness_action_packet.py --write-source-owner-packets --write-mvp-issue-packets
```

After credentials and redacted evidence are configured for a fresh run, run:

```bash
python3 scripts/spikes/live_readiness_preflight.py --strict --write-packet --write-spike-packets
```

### Evidence Manifest

- `evidence/readiness-manifest.json`

This manifest must declare the Feishu, model-provider, and archive-storage evidence files. It must also match the Model Provider run id/provider/model in `evidence/model-provider/usage-log.json`, the Archive/storage run id in `evidence/archive-storage/sync-result.json`, the current git `HEAD`, and a clean tracked worktree. Ignored `evidence/` files remain untracked.

Generate it after all live evidence files are present:

```bash
python3 scripts/spikes/readiness_manifest.py --write-final-review-packet
```

### Delivery Schedule

- `DELIVERY_DEADLINE_LOCAL_TIME`
- `DELIVERY_TIMEZONE`

Decision packet:

- `evidence/delivery-schedule-decision.md`

This input is runtime configuration rather than external live evidence, and the current secure local value is already configured. Use `HH:MM` for the local deadline with hour `00`-`23` and minute `00`-`59`, and use an IANA timezone such as `Asia/Shanghai`. The readiness gate validates both formats. Do not commit a real deployment value; configure it through `.env`, the Briefing Host secret/config store, or the Operations Console once implemented.

### Feishu

- `FEISHU_APP_ID`
- `FEISHU_APP_SECRET`
- `FEISHU_TENANT_KEY`, if required by the app setup
- `FEISHU_DEFAULT_USER_OPEN_ID`
- `FEISHU_DEFAULT_CHAT_ID`

Dry-run before live send:

```bash
python3 scripts/spikes/feishu_delivery_spike.py --dry-run
```

Live command after credentials are configured:

```bash
python3 scripts/spikes/feishu_delivery_spike.py
python3 scripts/spikes/feishu_delivery_spike.py --validate-evidence
```

Required evidence:

- Redacted internal-app request envelope for one user with `receive_id_type = open_id`.
- Redacted successful send response for one user.
- Redacted internal-app request envelope for one group with `receive_id_type = chat_id`.
- Redacted successful send response for one group.
- Rendered Feishu message text or screenshot.
- Scopes, permissions, recipient id type, rate/message-size notes, and any failure response shape.

Expected evidence files:

- `evidence/feishu-delivery/user-request.redacted.json`
- `evidence/feishu-delivery/user-response.redacted.json`
- `evidence/feishu-delivery/group-request.redacted.json`
- `evidence/feishu-delivery/group-response.redacted.json`
- `evidence/feishu-delivery/rendered-message.md`

### Model Provider

- `MODEL_PROVIDER`
- `MODEL_DEFAULT_MODEL`
- `MODEL_API_KEY`

Dry-run before live model calls:

```bash
python3 scripts/spikes/model_provider_spike.py --dry-run
python3 scripts/spikes/model_provider_spike.py --validate-requests
```

Validate redacted live evidence after calls are made with the chosen provider:

```bash
python3 scripts/spikes/model_provider_spike.py --validate-evidence
```

Required evidence:

- Redacted request/response for high-confidence news, low-confidence news, and academic paper examples.
- Provider/model name.
- Output run ids and output model usage provider/model values matching `usage-log.json`.
- Request count, token usage when available, latency, and failure reason when present.

Expected evidence files:

- `evidence/model-provider/outputs/high-confidence-news.json`
- `evidence/model-provider/outputs/low-confidence-news.json`
- `evidence/model-provider/outputs/academic-paper.json`
- `evidence/model-provider/usage-log.json`

### Archive Sync

- `ARCHIVE_LOCAL_ROOT`
- `ARCHIVE_SYNC_TARGET`

Dry-run before live sync:

```bash
python3 scripts/spikes/archive_storage_spike.py --dry-run
```

Live command after paths are configured:

```bash
python3 scripts/spikes/archive_storage_spike.py
python3 scripts/spikes/archive_storage_spike.py --validate-evidence
```

Required evidence:

- Local Archive Package path.
- Redacted sync command or host-native sync configuration note.
- Successful sync status for a sample package.
- Local and remote `file_count` values that match their corresponding tree file entries.
- Failure status remains recorded and retryable.

Expected evidence files:

- `evidence/archive-storage/sync-result.json`
- `evidence/archive-storage/local-tree.txt`
- `evidence/archive-storage/remote-tree.txt`

### Source Owner Approvals

Linked issue: #21.

No environment variables are required for this historical blocker. The current MVP source-set narrowing decision leaves `fixtures/source-ingestion/source-owner-review-queue.json` with 0 open items. If deferred sources are reopened later, the required external input is an owner/legal/product decision for each open source: explicit source permission, eligibility approval, blocking, deferral, or MVP source-set narrowing.

Use the ignored source-owner packets generated by:

```bash
python3 scripts/readiness_action_packet.py --write-source-owner-packets --write-mvp-issue-packets
```

Review packet paths:

- `evidence/source-owner-reviews/request-packet.md`
- `evidence/source-owner-reviews/worksheet.md`
- `evidence/source-owner-reviews/batch-plan.md`
- `evidence/source-owner-reviews/mvp-source-set-narrowing.md`

Commands after decisions are recorded:

```bash
python3 scripts/source_owner_review_decision.py --status
python3 scripts/source_owner_review_decision.py --validate-all
python3 scripts/source_owner_review_decision.py --apply-all --dry-run
python3 scripts/source_owner_review_decision.py --apply-all
python3 scripts/check_readiness.py
python3 scripts/check_readiness.py --require-github
```

Keep private permission notes or legal review details out of GitHub unless explicitly approved for sharing. Issue #21 may be closed after the final gate passes and copy-safe status is posted.

## Implementation Rule

Issues #10, #11, #12, #13, #14, #15, #16, #17, #18, and #19 can move to `ready-for-agent` after the final readiness and GitHub tracker gates pass. Issues #10, #11, #12, #13, and #14 are now complete; issues #15 through #19 remain `ready-for-agent`. Issue #20 remains `needs-triage` until the core MVP implementation issues are complete.
